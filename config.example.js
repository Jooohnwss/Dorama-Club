// Copie este arquivo para `config.js` e preencha com suas credenciais.
// `config.js` está no .gitignore e não vai pro repositório.

// TMDB — token de leitura (v4 auth). Pegue em https://www.themoviedb.org/settings/api
export const TMDB_BEARER = "COLE_AQUI_O_TOKEN_TMDB";

// Supabase — Settings > API do seu projeto.
export const SUPABASE_URL = "";
export const SUPABASE_ANON_KEY = "";
